# 04 · 脱稿验收脚本（不看任何教程）

**规则：一小时内，不查任何教程/代码，从空文件写出完整脚本**：
加载数据集 → 定义模型 → 训练循环 → 验证集评估。

允许查的只有 PyTorch 官方文档（当工具书）和自己之前的笔记。

写完后在文件头部用注释回答：
- 损失、梯度、学习率三者如何配合决定参数更新的方向与幅度？
- `optimizer.zero_grad()` 每轮必须调用的原因？

通过后，阶段 0 毕业 ✅
