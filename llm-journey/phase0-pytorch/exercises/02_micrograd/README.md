# 02 · micrograd 复现（本阶段核心练习）

两步走：
1. **跟敲**：跟随 Karpathy 视频（见阶段 0 README）逐行实现 `Value` 引擎 → `Neuron/Layer/MLP` → 反向传播 → 训练 demo；
2. **重写**：合上视频与本仓库 `micrograd/` 参考，从空文件独立重写，直到训练出同样的结果。

自测标准（重写完成后回答自己）：
- `Value` 的 `_backward` 闭包在什么时候被定义、什么时候被调用？
- 为什么拓扑排序是反着遍历？
- 学习率调大 10 倍会发生什么？为什么？
