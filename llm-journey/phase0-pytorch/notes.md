# 阶段 0 学习笔记

> 每学完一小节，花三五分钟写一条：**做了什么 / 发现了什么 / 卡在哪**。
> 不求完整，求连续。三个月后回看，这就是你最好的复习材料。

---

## 模板（复制使用）

```markdown
## 2026-XX-XX · 主题（如：micrograd Value 类）

**做了什么**：
**发现/学到**：
**卡住的问题**：
```

---

## 2026-08-31 · 阶段 0 环境准备（示例条目）

**做了什么**：建好 `llm-journey` 实验总仓，装好 Python 3.11 虚拟环境与 PyTorch（MPS 后端验证通过），克隆 micrograd 参考仓库，写好环境自检脚本。

**发现/学到**：d2l 1.0.3 锁定了老版本 numpy/scipy，和 Python 3.12 不兼容，须用 Python 3.11；Mac 上 PyTorch 用 `mps` 设备即可调用 Apple GPU。

**卡住的问题**：暂无。

---

## 2026-08-31 · 学习模式切换为「AI 助学」

**做了什么**：放弃"看视频 4h → 跟敲"的旧流程，改为场次制（S1–S9）：AI 摸底 → 预测式实验 → 禁写区构建 → 答辩验收。配套材料已入库 `tutor/`（prompt 卡、实验卡、答辩题库、5 例坏代码门诊——全部实测过）。

**发现/学到**：旧模式的病根是"接收时间占比过高 + 缺反馈回路"；AI 模式的护栏是禁写区（核心代码必须自己写）和"代码是仲裁者"（概念分歧跑代码裁决）。

**卡住的问题**：暂无。

---
